// const crypto = require('crypto') //Solo trabaja con el package.json de (type:commonjs)
import crypto from 'crypto' //Solo trabaja con el package.json de (type:module)
console.log('Hola Mundo');
console.log(Math.random());
console.log(crypto.randomUUID());
